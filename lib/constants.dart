import 'package:flutter/material.dart';

/// I have used this api to get Different products
String APIROOT = "https://fakestoreapi.com/products/";

/// list of colors that are used in this app
const bgColor = Colors.white;
const primary = Colors.indigo;
const secondary = Colors.orange;

/// our default Shadow
const shadow = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 25,
  color: Colors.black12,
);
